{
  "devDependencies": {
    "@commitlint/cli": "^17.0.0",
    "@commitlint/config-conventional": "^17.0.0",
    "hardhat": "^2.10.1",
    "prettier": "^2.5.1",
    "prettier-plugin-solidity": "^1.0.0-beta.19",
    "pretty-quick": "^3.1.3"
  },
  "scripts": {
    "lint": "prettier --write '**/*.sol' '**/*.json'",
    "lint:check": "prettier --check '**/*.sol' ''**/*.json''"
  }
}
